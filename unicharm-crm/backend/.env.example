PORT=4000
# Must match frontend JWT_SECRET. Generate: openssl rand -base64 32
JWT_SECRET="change-me"
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
FRONTEND_ORIGIN="http://localhost:3000"
